# Deployed Google Apps Script Web App URL (ends with /exec).
# Copy this file to .env and fill in the real URL.
VITE_WAITLIST_ENDPOINT=

# Google Analytics 4 measurement id (Admin → Data Streams → Web → "G-XXXXXXXXXX").
# Leave empty to disable analytics (local dev sends nothing).
VITE_GA_MEASUREMENT_ID=
